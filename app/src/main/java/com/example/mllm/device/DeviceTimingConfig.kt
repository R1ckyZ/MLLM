package com.example.mllm.device

object DeviceTimingConfig {
    const val defaultTapDelayMs: Long = 1000L
    const val defaultDoubleTapDelayMs: Long = 1000L
    const val doubleTapIntervalMs: Long = 100L
    const val defaultLongPressDelayMs: Long = 1000L
    const val defaultSwipeDelayMs: Long = 1000L
    const val defaultBackDelayMs: Long = 1000L
    const val defaultHomeDelayMs: Long = 1000L
    const val defaultLaunchDelayMs: Long = 1000L
}
